extends ../layout

block content
    h1.
         #{title}
    ul
        - each blob, i in blobs
            li
                = blob.name
                = blob.badge
                = blob.dob
                = blob.isloved
                = blob._id
                form(action='/blobs/#{blob._id}/edit',method='post',enctype='application/x-www-form-urlencoded')
                    input(type='hidden',value='DELETE',name='_method')
                    button(type='submit').
                        삭제
                p 
                    a(href='/blobs/#{blob._id}/edit') 수정
                p
                    a(href='/blobs/#{blob._id}') 내용보기